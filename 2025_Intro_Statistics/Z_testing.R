2*(1-pnorm(3.5, mean=3, sd=0.1))

# Given values
sample_mean <- 25.81
population_mean <- 25.3
population_sd <- 1.2
sample_size <- 16

# Z-value formula
z_value <- (sample_mean - population_mean) / (population_sd / sqrt(sample_size))
z_value

